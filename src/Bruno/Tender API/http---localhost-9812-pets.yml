info:
  name: http://localhost:9812/pets
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:9812/pets?species=blob
  params:
    - name: species
      value: blob
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
